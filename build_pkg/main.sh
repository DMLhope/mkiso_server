source ./build_pkg.conf

git_url=$git_url
branch_name=$branch_name
sudo_passwd=$sudo_passwd

work_path=$work_path
arch=""

mode=$1

case mode in
    git_pkg)
    
    ;;
    dsc_pkg)

    ;;
    list_pkg)

    ;;
    *)

    ;;
esac